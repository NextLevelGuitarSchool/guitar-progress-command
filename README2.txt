NLG Student Console — corrected logo and raised top frame

Upload only index.html to the root of the GitHub Pages repository and replace the existing file.

The NLG logo is embedded inside index.html, so no separate logo image is required.
The centre logo housing now extends above the top edge of the console and is no longer clipped.
